// Generated by svd2swift.

import MMIO

/// I2C compatible Two-Wire Slave Interface with EasyDMA 0
@RegisterBlock
struct TWIS0 {
    /// Stop TWI transaction
    @RegisterBlock(offset: 0x14)
    var tasks_stop: Register<TASKS_STOP>

    /// Suspend TWI transaction
    @RegisterBlock(offset: 0x1c)
    var tasks_suspend: Register<TASKS_SUSPEND>

    /// Resume TWI transaction
    @RegisterBlock(offset: 0x20)
    var tasks_resume: Register<TASKS_RESUME>

    /// Prepare the TWI slave to respond to a write command
    @RegisterBlock(offset: 0x30)
    var tasks_preparerx: Register<TASKS_PREPARERX>

    /// Prepare the TWI slave to respond to a read command
    @RegisterBlock(offset: 0x34)
    var tasks_preparetx: Register<TASKS_PREPARETX>

    /// TWI stopped
    @RegisterBlock(offset: 0x104)
    var events_stopped: Register<EVENTS_STOPPED>

    /// TWI error
    @RegisterBlock(offset: 0x124)
    var events_error: Register<EVENTS_ERROR>

    /// Receive sequence started
    @RegisterBlock(offset: 0x14c)
    var events_rxstarted: Register<EVENTS_RXSTARTED>

    /// Transmit sequence started
    @RegisterBlock(offset: 0x150)
    var events_txstarted: Register<EVENTS_TXSTARTED>

    /// Write command received
    @RegisterBlock(offset: 0x164)
    var events_write: Register<EVENTS_WRITE>

    /// Read command received
    @RegisterBlock(offset: 0x168)
    var events_read: Register<EVENTS_READ>

    /// Shortcuts between local events and tasks
    @RegisterBlock(offset: 0x200)
    var shorts: Register<SHORTS>

    /// Enable or disable interrupt
    @RegisterBlock(offset: 0x300)
    var inten: Register<INTEN>

    /// Enable interrupt
    @RegisterBlock(offset: 0x304)
    var intenset: Register<INTENSET>

    /// Disable interrupt
    @RegisterBlock(offset: 0x308)
    var intenclr: Register<INTENCLR>

    /// Error source
    @RegisterBlock(offset: 0x4d0)
    var errorsrc: Register<ERRORSRC>

    /// Status register indicating which address had a match
    @RegisterBlock(offset: 0x4d4)
    var match: Register<MATCH>

    /// Enable TWIS
    @RegisterBlock(offset: 0x500)
    var enable: Register<ENABLE>

    /// Description collection: TWI slave address n
    @RegisterBlock(offset: 0x588, stride: 0x4, count: 2)
    var address: RegisterArray<ADDRESS>

    /// Configuration register for the address match mechanism
    @RegisterBlock(offset: 0x594)
    var config: Register<CONFIG>

    /// Over-read character. Character sent out in case of an over-read of the transmit buffer.
    @RegisterBlock(offset: 0x5c0)
    var orc: Register<ORC>

    /// Unspecified
    @RegisterBlock(offset: 0x508)
    var psel: PSEL

    /// RXD EasyDMA channel
    @RegisterBlock(offset: 0x534)
    var rxd: RXD

    /// TXD EasyDMA channel
    @RegisterBlock(offset: 0x544)
    var txd: TXD
}

extension TWIS0 {
    /// Stop TWI transaction
    @Register(bitWidth: 32)
    struct TASKS_STOP {
        /// Stop TWI transaction
        @WriteOnly(bits: 0..<1, as: TASKS_STOP_FIELDValues.self)
        var tasks_stop_field: TASKS_STOP_FIELD
    }

    /// Suspend TWI transaction
    @Register(bitWidth: 32)
    struct TASKS_SUSPEND {
        /// Suspend TWI transaction
        @WriteOnly(bits: 0..<1, as: TASKS_SUSPEND_FIELDValues.self)
        var tasks_suspend_field: TASKS_SUSPEND_FIELD
    }

    /// Resume TWI transaction
    @Register(bitWidth: 32)
    struct TASKS_RESUME {
        /// Resume TWI transaction
        @WriteOnly(bits: 0..<1, as: TASKS_RESUME_FIELDValues.self)
        var tasks_resume_field: TASKS_RESUME_FIELD
    }

    /// Prepare the TWI slave to respond to a write command
    @Register(bitWidth: 32)
    struct TASKS_PREPARERX {
        /// Prepare the TWI slave to respond to a write command
        @WriteOnly(bits: 0..<1, as: TASKS_PREPARERX_FIELDValues.self)
        var tasks_preparerx_field: TASKS_PREPARERX_FIELD
    }

    /// Prepare the TWI slave to respond to a read command
    @Register(bitWidth: 32)
    struct TASKS_PREPARETX {
        /// Prepare the TWI slave to respond to a read command
        @WriteOnly(bits: 0..<1, as: TASKS_PREPARETX_FIELDValues.self)
        var tasks_preparetx_field: TASKS_PREPARETX_FIELD
    }

    /// TWI stopped
    @Register(bitWidth: 32)
    struct EVENTS_STOPPED {
        /// TWI stopped
        @ReadWrite(bits: 0..<1, as: EVENTS_STOPPED_FIELDValues.self)
        var events_stopped_field: EVENTS_STOPPED_FIELD
    }

    /// TWI error
    @Register(bitWidth: 32)
    struct EVENTS_ERROR {
        /// TWI error
        @ReadWrite(bits: 0..<1, as: EVENTS_ERROR_FIELDValues.self)
        var events_error_field: EVENTS_ERROR_FIELD
    }

    /// Receive sequence started
    @Register(bitWidth: 32)
    struct EVENTS_RXSTARTED {
        /// Receive sequence started
        @ReadWrite(bits: 0..<1, as: EVENTS_RXSTARTED_FIELDValues.self)
        var events_rxstarted_field: EVENTS_RXSTARTED_FIELD
    }

    /// Transmit sequence started
    @Register(bitWidth: 32)
    struct EVENTS_TXSTARTED {
        /// Transmit sequence started
        @ReadWrite(bits: 0..<1, as: EVENTS_TXSTARTED_FIELDValues.self)
        var events_txstarted_field: EVENTS_TXSTARTED_FIELD
    }

    /// Write command received
    @Register(bitWidth: 32)
    struct EVENTS_WRITE {
        /// Write command received
        @ReadWrite(bits: 0..<1, as: EVENTS_WRITE_FIELDValues.self)
        var events_write_field: EVENTS_WRITE_FIELD
    }

    /// Read command received
    @Register(bitWidth: 32)
    struct EVENTS_READ {
        /// Read command received
        @ReadWrite(bits: 0..<1, as: EVENTS_READ_FIELDValues.self)
        var events_read_field: EVENTS_READ_FIELD
    }

    /// Shortcuts between local events and tasks
    @Register(bitWidth: 32)
    struct SHORTS {
        /// Shortcut between event WRITE and task SUSPEND
        @ReadWrite(bits: 13..<14, as: WRITE_SUSPENDValues.self)
        var write_suspend: WRITE_SUSPEND

        /// Shortcut between event READ and task SUSPEND
        @ReadWrite(bits: 14..<15, as: READ_SUSPENDValues.self)
        var read_suspend: READ_SUSPEND
    }

    /// Enable or disable interrupt
    @Register(bitWidth: 32)
    struct INTEN {
        /// Enable or disable interrupt for event STOPPED
        @ReadWrite(bits: 1..<2, as: STOPPEDValues.self)
        var stopped: STOPPED

        /// Enable or disable interrupt for event ERROR
        @ReadWrite(bits: 9..<10, as: ERRORValues.self)
        var error: ERROR

        /// Enable or disable interrupt for event RXSTARTED
        @ReadWrite(bits: 19..<20, as: RXSTARTEDValues.self)
        var rxstarted: RXSTARTED

        /// Enable or disable interrupt for event TXSTARTED
        @ReadWrite(bits: 20..<21, as: TXSTARTEDValues.self)
        var txstarted: TXSTARTED

        /// Enable or disable interrupt for event WRITE
        @ReadWrite(bits: 25..<26, as: WRITEValues.self)
        var write: WRITE

        /// Enable or disable interrupt for event READ
        @ReadWrite(bits: 26..<27, as: READValues.self)
        var read: READ
    }

    /// Enable interrupt
    @Register(bitWidth: 32)
    struct INTENSET {
        /// Write '1' to enable interrupt for event STOPPED
        @ReadWrite(bits: 1..<2)
        var stopped: STOPPED

        /// Write '1' to enable interrupt for event ERROR
        @ReadWrite(bits: 9..<10)
        var error: ERROR

        /// Write '1' to enable interrupt for event RXSTARTED
        @ReadWrite(bits: 19..<20)
        var rxstarted: RXSTARTED

        /// Write '1' to enable interrupt for event TXSTARTED
        @ReadWrite(bits: 20..<21)
        var txstarted: TXSTARTED

        /// Write '1' to enable interrupt for event WRITE
        @ReadWrite(bits: 25..<26)
        var write: WRITE

        /// Write '1' to enable interrupt for event READ
        @ReadWrite(bits: 26..<27)
        var read: READ
    }

    /// Disable interrupt
    @Register(bitWidth: 32)
    struct INTENCLR {
        /// Write '1' to disable interrupt for event STOPPED
        @ReadWrite(bits: 1..<2)
        var stopped: STOPPED

        /// Write '1' to disable interrupt for event ERROR
        @ReadWrite(bits: 9..<10)
        var error: ERROR

        /// Write '1' to disable interrupt for event RXSTARTED
        @ReadWrite(bits: 19..<20)
        var rxstarted: RXSTARTED

        /// Write '1' to disable interrupt for event TXSTARTED
        @ReadWrite(bits: 20..<21)
        var txstarted: TXSTARTED

        /// Write '1' to disable interrupt for event WRITE
        @ReadWrite(bits: 25..<26)
        var write: WRITE

        /// Write '1' to disable interrupt for event READ
        @ReadWrite(bits: 26..<27)
        var read: READ
    }

    /// Error source
    @Register(bitWidth: 32)
    struct ERRORSRC {
        /// RX buffer overflow detected, and prevented
        @ReadWrite(bits: 0..<1, as: OVERFLOWValues.self)
        var overflow: OVERFLOW

        /// NACK sent after receiving a data byte
        @ReadWrite(bits: 2..<3, as: DNACKValues.self)
        var dnack: DNACK

        /// TX buffer over-read detected, and prevented
        @ReadWrite(bits: 3..<4, as: OVERREADValues.self)
        var overread: OVERREAD
    }

    /// Status register indicating which address had a match
    @Register(bitWidth: 32)
    struct MATCH {
        /// Indication of which address in {ADDRESS} that matched the incoming address
        @ReadOnly(bits: 0..<1)
        var match_field: MATCH_FIELD
    }

    /// Enable TWIS
    @Register(bitWidth: 32)
    struct ENABLE {
        /// Enable or disable TWIS
        @ReadWrite(bits: 0..<4, as: ENABLE_FIELDValues.self)
        var enable_field: ENABLE_FIELD
    }

    /// Description collection: TWI slave address n
    @Register(bitWidth: 32)
    struct ADDRESS {
        /// TWI slave address
        @ReadWrite(bits: 0..<7)
        var address_field: ADDRESS_FIELD
    }

    /// Configuration register for the address match mechanism
    @Register(bitWidth: 32)
    struct CONFIG {
        /// Enable or disable address matching on ADDRESS[0]
        @ReadWrite(bits: 0..<1, as: ADDRESS0Values.self)
        var address0: ADDRESS0

        /// Enable or disable address matching on ADDRESS[1]
        @ReadWrite(bits: 1..<2, as: ADDRESS1Values.self)
        var address1: ADDRESS1
    }

    /// Over-read character. Character sent out in case of an over-read of the transmit buffer.
    @Register(bitWidth: 32)
    struct ORC {
        /// Over-read character. Character sent out in case of an over-read of the transmit buffer.
        @ReadWrite(bits: 0..<8)
        var orc_field: ORC_FIELD
    }

    /// Unspecified
    @RegisterBlock
    struct PSEL {
        /// Pin select for SCL signal
        @RegisterBlock(offset: 0x0)
        var scl: Register<SCL>

        /// Pin select for SDA signal
        @RegisterBlock(offset: 0x4)
        var sda: Register<SDA>
    }

    /// RXD EasyDMA channel
    @RegisterBlock
    struct RXD {
        /// RXD Data pointer
        @RegisterBlock(offset: 0x0)
        var ptr: Register<PTR>

        /// Maximum number of bytes in RXD buffer
        @RegisterBlock(offset: 0x4)
        var maxcnt: Register<MAXCNT>

        /// Number of bytes transferred in the last RXD transaction
        @RegisterBlock(offset: 0x8)
        var amount: Register<AMOUNT>

        /// EasyDMA list type
        @RegisterBlock(offset: 0xc)
        var list: Register<LIST>
    }

    /// TXD EasyDMA channel
    @RegisterBlock
    struct TXD {
        /// TXD Data pointer
        @RegisterBlock(offset: 0x0)
        var ptr: Register<PTR>

        /// Maximum number of bytes in TXD buffer
        @RegisterBlock(offset: 0x4)
        var maxcnt: Register<MAXCNT>

        /// Number of bytes transferred in the last TXD transaction
        @RegisterBlock(offset: 0x8)
        var amount: Register<AMOUNT>

        /// EasyDMA list type
        @RegisterBlock(offset: 0xc)
        var list: Register<LIST>
    }
}

extension TWIS0.PSEL {
    /// Pin select for SCL signal
    @Register(bitWidth: 32)
    struct SCL {
        /// Pin number
        @ReadWrite(bits: 0..<5)
        var pin: PIN

        /// Port number
        @ReadWrite(bits: 5..<6)
        var port: PORT

        /// Connection
        @ReadWrite(bits: 31..<32, as: CONNECTValues.self)
        var connect: CONNECT
    }

    /// Pin select for SDA signal
    @Register(bitWidth: 32)
    struct SDA {
        /// Pin number
        @ReadWrite(bits: 0..<5)
        var pin: PIN

        /// Port number
        @ReadWrite(bits: 5..<6)
        var port: PORT

        /// Connection
        @ReadWrite(bits: 31..<32, as: CONNECTValues.self)
        var connect: CONNECT
    }
}

extension TWIS0.RXD {
    /// RXD Data pointer
    @Register(bitWidth: 32)
    struct PTR {
        /// RXD Data pointer
        @ReadWrite(bits: 0..<32)
        var ptr_field: PTR_FIELD
    }

    /// Maximum number of bytes in RXD buffer
    @Register(bitWidth: 32)
    struct MAXCNT {
        /// Maximum number of bytes in RXD buffer
        @ReadWrite(bits: 0..<16)
        var maxcnt_field: MAXCNT_FIELD
    }

    /// Number of bytes transferred in the last RXD transaction
    @Register(bitWidth: 32)
    struct AMOUNT {
        /// Number of bytes transferred in the last RXD transaction
        @ReadOnly(bits: 0..<16)
        var amount_field: AMOUNT_FIELD
    }

    /// EasyDMA list type
    @Register(bitWidth: 32)
    struct LIST {
        /// List type
        @ReadWrite(bits: 0..<2, as: LIST_FIELDValues.self)
        var list_field: LIST_FIELD
    }
}

extension TWIS0.TXD {
    /// TXD Data pointer
    @Register(bitWidth: 32)
    struct PTR {
        /// TXD Data pointer
        @ReadWrite(bits: 0..<32)
        var ptr_field: PTR_FIELD
    }

    /// Maximum number of bytes in TXD buffer
    @Register(bitWidth: 32)
    struct MAXCNT {
        /// Maximum number of bytes in TXD buffer
        @ReadWrite(bits: 0..<16)
        var maxcnt_field: MAXCNT_FIELD
    }

    /// Number of bytes transferred in the last TXD transaction
    @Register(bitWidth: 32)
    struct AMOUNT {
        /// Number of bytes transferred in the last TXD transaction
        @ReadOnly(bits: 0..<16)
        var amount_field: AMOUNT_FIELD
    }

    /// EasyDMA list type
    @Register(bitWidth: 32)
    struct LIST {
        /// List type
        @ReadWrite(bits: 0..<2, as: LIST_FIELDValues.self)
        var list_field: LIST_FIELD
    }
}

extension TWIS0.TASKS_STOP {
    struct TASKS_STOP_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.TASKS_SUSPEND {
    struct TASKS_SUSPEND_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.TASKS_RESUME {
    struct TASKS_RESUME_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.TASKS_PREPARERX {
    struct TASKS_PREPARERX_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.TASKS_PREPARETX {
    struct TASKS_PREPARETX_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.EVENTS_STOPPED {
    struct EVENTS_STOPPED_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.EVENTS_ERROR {
    struct EVENTS_ERROR_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.EVENTS_RXSTARTED {
    struct EVENTS_RXSTARTED_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.EVENTS_TXSTARTED {
    struct EVENTS_TXSTARTED_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.EVENTS_WRITE {
    struct EVENTS_WRITE_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.EVENTS_READ {
    struct EVENTS_READ_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.SHORTS {
    struct WRITE_SUSPENDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable shortcut
        static let Disabled = Self(rawValue: 0x0)

        /// Enable shortcut
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.SHORTS {
    struct READ_SUSPENDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable shortcut
        static let Disabled = Self(rawValue: 0x0)

        /// Enable shortcut
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.INTEN {
    struct STOPPEDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.INTEN {
    struct ERRORValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.INTEN {
    struct RXSTARTEDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.INTEN {
    struct TXSTARTEDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.INTEN {
    struct WRITEValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.INTEN {
    struct READValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.ERRORSRC {
    struct OVERFLOWValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Error did not occur
        static let NotDetected = Self(rawValue: 0x0)

        /// Error occurred
        static let Detected = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.ERRORSRC {
    struct DNACKValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Error did not occur
        static let NotReceived = Self(rawValue: 0x0)

        /// Error occurred
        static let Received = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.ERRORSRC {
    struct OVERREADValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Error did not occur
        static let NotDetected = Self(rawValue: 0x0)

        /// Error occurred
        static let Detected = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.ENABLE {
    struct ENABLE_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 4

        /// Disable TWIS
        static let Disabled = Self(rawValue: 0x0)

        /// Enable TWIS
        static let Enabled = Self(rawValue: 0x9)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.CONFIG {
    struct ADDRESS0Values: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disabled
        static let Disabled = Self(rawValue: 0x0)

        /// Enabled
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.CONFIG {
    struct ADDRESS1Values: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disabled
        static let Disabled = Self(rawValue: 0x0)

        /// Enabled
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.PSEL.SCL {
    struct CONNECTValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disconnect
        static let Disconnected = Self(rawValue: 0x1)

        /// Connect
        static let Connected = Self(rawValue: 0x0)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.PSEL.SDA {
    struct CONNECTValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disconnect
        static let Disconnected = Self(rawValue: 0x1)

        /// Connect
        static let Connected = Self(rawValue: 0x0)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.RXD.LIST {
    struct LIST_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 2

        /// Disable EasyDMA list
        static let Disabled = Self(rawValue: 0x0)

        /// Use array list
        static let ArrayList = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension TWIS0.TXD.LIST {
    struct LIST_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 2

        /// Disable EasyDMA list
        static let Disabled = Self(rawValue: 0x0)

        /// Use array list
        static let ArrayList = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}
