// Generated by svd2swift.

import MMIO

/// GPIO Tasks and Events
@RegisterBlock
struct GPIOTE {
    /// Description collection: Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is configured in CONFIG[n].POLARITY.
    @RegisterBlock(offset: 0x0, stride: 0x4, count: 8)
    var tasks_out: RegisterArray<TASKS_OUT>

    /// Description collection: Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is to set it high.
    @RegisterBlock(offset: 0x30, stride: 0x4, count: 8)
    var tasks_set: RegisterArray<TASKS_SET>

    /// Description collection: Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is to set it low.
    @RegisterBlock(offset: 0x60, stride: 0x4, count: 8)
    var tasks_clr: RegisterArray<TASKS_CLR>

    /// Description collection: Event generated from pin specified in CONFIG[n].PSEL
    @RegisterBlock(offset: 0x100, stride: 0x4, count: 8)
    var events_in: RegisterArray<EVENTS_IN>

    /// Event generated from multiple input GPIO pins with SENSE mechanism enabled
    @RegisterBlock(offset: 0x17c)
    var events_port: Register<EVENTS_PORT>

    /// Enable interrupt
    @RegisterBlock(offset: 0x304)
    var intenset: Register<INTENSET>

    /// Disable interrupt
    @RegisterBlock(offset: 0x308)
    var intenclr: Register<INTENCLR>

    /// Description collection: Configuration for OUT[n], SET[n], and CLR[n] tasks and IN[n] event
    @RegisterBlock(offset: 0x510, stride: 0x4, count: 8)
    var config: RegisterArray<CONFIG>
}

extension GPIOTE {
    /// Description collection: Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is configured in CONFIG[n].POLARITY.
    @Register(bitWidth: 32)
    struct TASKS_OUT {
        /// Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is configured in CONFIG[n].POLARITY.
        @WriteOnly(bits: 0..<1, as: TASKS_OUT_FIELDValues.self)
        var tasks_out_field: TASKS_OUT_FIELD
    }

    /// Description collection: Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is to set it high.
    @Register(bitWidth: 32)
    struct TASKS_SET {
        /// Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is to set it high.
        @WriteOnly(bits: 0..<1, as: TASKS_SET_FIELDValues.self)
        var tasks_set_field: TASKS_SET_FIELD
    }

    /// Description collection: Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is to set it low.
    @Register(bitWidth: 32)
    struct TASKS_CLR {
        /// Task for writing to pin specified in CONFIG[n].PSEL. Action on pin is to set it low.
        @WriteOnly(bits: 0..<1, as: TASKS_CLR_FIELDValues.self)
        var tasks_clr_field: TASKS_CLR_FIELD
    }

    /// Description collection: Event generated from pin specified in CONFIG[n].PSEL
    @Register(bitWidth: 32)
    struct EVENTS_IN {
        /// Event generated from pin specified in CONFIG[n].PSEL
        @ReadWrite(bits: 0..<1, as: EVENTS_IN_FIELDValues.self)
        var events_in_field: EVENTS_IN_FIELD
    }

    /// Event generated from multiple input GPIO pins with SENSE mechanism enabled
    @Register(bitWidth: 32)
    struct EVENTS_PORT {
        /// Event generated from multiple input GPIO pins with SENSE mechanism enabled
        @ReadWrite(bits: 0..<1, as: EVENTS_PORT_FIELDValues.self)
        var events_port_field: EVENTS_PORT_FIELD
    }

    /// Enable interrupt
    @Register(bitWidth: 32)
    struct INTENSET {
        /// Write '1' to enable interrupt for event IN[0]
        @ReadWrite(bits: 0..<1)
        var in0: IN0

        /// Write '1' to enable interrupt for event IN[1]
        @ReadWrite(bits: 1..<2)
        var in1: IN1

        /// Write '1' to enable interrupt for event IN[2]
        @ReadWrite(bits: 2..<3)
        var in2: IN2

        /// Write '1' to enable interrupt for event IN[3]
        @ReadWrite(bits: 3..<4)
        var in3: IN3

        /// Write '1' to enable interrupt for event IN[4]
        @ReadWrite(bits: 4..<5)
        var in4: IN4

        /// Write '1' to enable interrupt for event IN[5]
        @ReadWrite(bits: 5..<6)
        var in5: IN5

        /// Write '1' to enable interrupt for event IN[6]
        @ReadWrite(bits: 6..<7)
        var in6: IN6

        /// Write '1' to enable interrupt for event IN[7]
        @ReadWrite(bits: 7..<8)
        var in7: IN7

        /// Write '1' to enable interrupt for event PORT
        @ReadWrite(bits: 31..<32)
        var port: PORT
    }

    /// Disable interrupt
    @Register(bitWidth: 32)
    struct INTENCLR {
        /// Write '1' to disable interrupt for event IN[0]
        @ReadWrite(bits: 0..<1)
        var in0: IN0

        /// Write '1' to disable interrupt for event IN[1]
        @ReadWrite(bits: 1..<2)
        var in1: IN1

        /// Write '1' to disable interrupt for event IN[2]
        @ReadWrite(bits: 2..<3)
        var in2: IN2

        /// Write '1' to disable interrupt for event IN[3]
        @ReadWrite(bits: 3..<4)
        var in3: IN3

        /// Write '1' to disable interrupt for event IN[4]
        @ReadWrite(bits: 4..<5)
        var in4: IN4

        /// Write '1' to disable interrupt for event IN[5]
        @ReadWrite(bits: 5..<6)
        var in5: IN5

        /// Write '1' to disable interrupt for event IN[6]
        @ReadWrite(bits: 6..<7)
        var in6: IN6

        /// Write '1' to disable interrupt for event IN[7]
        @ReadWrite(bits: 7..<8)
        var in7: IN7

        /// Write '1' to disable interrupt for event PORT
        @ReadWrite(bits: 31..<32)
        var port: PORT
    }

    /// Description collection: Configuration for OUT[n], SET[n], and CLR[n] tasks and IN[n] event
    @Register(bitWidth: 32)
    struct CONFIG {
        /// Mode
        @ReadWrite(bits: 0..<2, as: MODEValues.self)
        var mode: MODE

        /// GPIO number associated with SET[n], CLR[n], and OUT[n] tasks and IN[n] event
        @ReadWrite(bits: 8..<13)
        var psel: PSEL

        /// Port number
        @ReadWrite(bits: 13..<14)
        var port: PORT

        /// When In task mode: Operation to be performed on output when OUT[n] task is triggered. When In event mode: Operation on input that shall trigger IN[n] event.
        @ReadWrite(bits: 16..<18, as: POLARITYValues.self)
        var polarity: POLARITY

        /// When in task mode: Initial value of the output when the GPIOTE channel is configured. When in event mode: No effect.
        @ReadWrite(bits: 20..<21, as: OUTINITValues.self)
        var outinit: OUTINIT
    }
}

extension GPIOTE.TASKS_OUT {
    struct TASKS_OUT_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.TASKS_SET {
    struct TASKS_SET_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.TASKS_CLR {
    struct TASKS_CLR_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.EVENTS_IN {
    struct EVENTS_IN_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.EVENTS_PORT {
    struct EVENTS_PORT_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.CONFIG {
    struct MODEValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 2

        /// Disabled. Pin specified by PSEL will not be acquired by the GPIOTE module.
        static let Disabled = Self(rawValue: 0x0)

        /// Event mode
        static let Event = Self(rawValue: 0x1)

        /// Task mode
        static let Task = Self(rawValue: 0x3)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.CONFIG {
    struct POLARITYValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 2

        /// Task mode: No effect on pin from OUT[n] task. Event mode: no IN[n] event generated on pin activity.
        static let None = Self(rawValue: 0x0)

        /// Task mode: Set pin from OUT[n] task. Event mode: Generate IN[n] event when rising edge on pin.
        static let LoToHi = Self(rawValue: 0x1)

        /// Task mode: Clear pin from OUT[n] task. Event mode: Generate IN[n] event when falling edge on pin.
        static let HiToLo = Self(rawValue: 0x2)

        /// Task mode: Toggle pin from OUT[n]. Event mode: Generate IN[n] when any change on pin.
        static let Toggle = Self(rawValue: 0x3)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension GPIOTE.CONFIG {
    struct OUTINITValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Task mode: Initial value of pin before task triggering is low
        static let Low = Self(rawValue: 0x0)

        /// Task mode: Initial value of pin before task triggering is high
        static let High = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}
