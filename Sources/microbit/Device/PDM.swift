// Generated by svd2swift.

import MMIO

/// Pulse Density Modulation (Digital Microphone) Interface
@RegisterBlock
struct PDM {
    /// Starts continuous PDM transfer
    @RegisterBlock(offset: 0x0)
    var tasks_start: Register<TASKS_START>

    /// Stops PDM transfer
    @RegisterBlock(offset: 0x4)
    var tasks_stop: Register<TASKS_STOP>

    /// PDM transfer has started
    @RegisterBlock(offset: 0x100)
    var events_started: Register<EVENTS_STARTED>

    /// PDM transfer has finished
    @RegisterBlock(offset: 0x104)
    var events_stopped: Register<EVENTS_STOPPED>

    /// The PDM has written the last sample specified by SAMPLE.MAXCNT (or the last sample after a STOP task has been received) to Data RAM
    @RegisterBlock(offset: 0x108)
    var events_end: Register<EVENTS_END>

    /// Enable or disable interrupt
    @RegisterBlock(offset: 0x300)
    var inten: Register<INTEN>

    /// Enable interrupt
    @RegisterBlock(offset: 0x304)
    var intenset: Register<INTENSET>

    /// Disable interrupt
    @RegisterBlock(offset: 0x308)
    var intenclr: Register<INTENCLR>

    /// PDM module enable register
    @RegisterBlock(offset: 0x500)
    var enable: Register<ENABLE>

    /// PDM clock generator control
    @RegisterBlock(offset: 0x504)
    var pdmclkctrl: Register<PDMCLKCTRL>

    /// Defines the routing of the connected PDM microphones' signals
    @RegisterBlock(offset: 0x508)
    var mode: Register<MODE>

    /// Left output gain adjustment
    @RegisterBlock(offset: 0x518)
    var gainl: Register<GAINL>

    /// Right output gain adjustment
    @RegisterBlock(offset: 0x51c)
    var gainr: Register<GAINR>

    /// Selects the ratio between PDM_CLK and output sample rate. Change PDMCLKCTRL accordingly.
    @RegisterBlock(offset: 0x520)
    var ratio: Register<RATIO>

    /// Unspecified
    @RegisterBlock(offset: 0x540)
    var psel: PSEL

    /// Unspecified
    @RegisterBlock(offset: 0x560)
    var sample: SAMPLE
}

extension PDM {
    /// Starts continuous PDM transfer
    @Register(bitWidth: 32)
    struct TASKS_START {
        /// Starts continuous PDM transfer
        @WriteOnly(bits: 0..<1, as: TASKS_START_FIELDValues.self)
        var tasks_start_field: TASKS_START_FIELD
    }

    /// Stops PDM transfer
    @Register(bitWidth: 32)
    struct TASKS_STOP {
        /// Stops PDM transfer
        @WriteOnly(bits: 0..<1, as: TASKS_STOP_FIELDValues.self)
        var tasks_stop_field: TASKS_STOP_FIELD
    }

    /// PDM transfer has started
    @Register(bitWidth: 32)
    struct EVENTS_STARTED {
        /// PDM transfer has started
        @ReadWrite(bits: 0..<1, as: EVENTS_STARTED_FIELDValues.self)
        var events_started_field: EVENTS_STARTED_FIELD
    }

    /// PDM transfer has finished
    @Register(bitWidth: 32)
    struct EVENTS_STOPPED {
        /// PDM transfer has finished
        @ReadWrite(bits: 0..<1, as: EVENTS_STOPPED_FIELDValues.self)
        var events_stopped_field: EVENTS_STOPPED_FIELD
    }

    /// The PDM has written the last sample specified by SAMPLE.MAXCNT (or the last sample after a STOP task has been received) to Data RAM
    @Register(bitWidth: 32)
    struct EVENTS_END {
        /// The PDM has written the last sample specified by SAMPLE.MAXCNT (or the last sample after a STOP task has been received) to Data RAM
        @ReadWrite(bits: 0..<1, as: EVENTS_END_FIELDValues.self)
        var events_end_field: EVENTS_END_FIELD
    }

    /// Enable or disable interrupt
    @Register(bitWidth: 32)
    struct INTEN {
        /// Enable or disable interrupt for event STARTED
        @ReadWrite(bits: 0..<1, as: STARTEDValues.self)
        var started: STARTED

        /// Enable or disable interrupt for event STOPPED
        @ReadWrite(bits: 1..<2, as: STOPPEDValues.self)
        var stopped: STOPPED

        /// Enable or disable interrupt for event END
        @ReadWrite(bits: 2..<3, as: ENDValues.self)
        var end: END
    }

    /// Enable interrupt
    @Register(bitWidth: 32)
    struct INTENSET {
        /// Write '1' to enable interrupt for event STARTED
        @ReadWrite(bits: 0..<1)
        var started: STARTED

        /// Write '1' to enable interrupt for event STOPPED
        @ReadWrite(bits: 1..<2)
        var stopped: STOPPED

        /// Write '1' to enable interrupt for event END
        @ReadWrite(bits: 2..<3)
        var end: END
    }

    /// Disable interrupt
    @Register(bitWidth: 32)
    struct INTENCLR {
        /// Write '1' to disable interrupt for event STARTED
        @ReadWrite(bits: 0..<1)
        var started: STARTED

        /// Write '1' to disable interrupt for event STOPPED
        @ReadWrite(bits: 1..<2)
        var stopped: STOPPED

        /// Write '1' to disable interrupt for event END
        @ReadWrite(bits: 2..<3)
        var end: END
    }

    /// PDM module enable register
    @Register(bitWidth: 32)
    struct ENABLE {
        /// Enable or disable PDM module
        @ReadWrite(bits: 0..<1, as: ENABLE_FIELDValues.self)
        var enable_field: ENABLE_FIELD
    }

    /// PDM clock generator control
    @Register(bitWidth: 32)
    struct PDMCLKCTRL {
        /// PDM_CLK frequency configuration
        @ReadWrite(bits: 0..<32, as: FREQValues.self)
        var freq: FREQ
    }

    /// Defines the routing of the connected PDM microphones' signals
    @Register(bitWidth: 32)
    struct MODE {
        /// Mono or stereo operation
        @ReadWrite(bits: 0..<1, as: OPERATIONValues.self)
        var operation: OPERATION

        /// Defines on which PDM_CLK edge left (or mono) is sampled
        @ReadWrite(bits: 1..<2, as: EDGEValues.self)
        var edge: EDGE
    }

    /// Left output gain adjustment
    @Register(bitWidth: 32)
    struct GAINL {
        /// Left output gain adjustment, in 0.5 dB steps, around the default module gain (see electrical parameters) 0x00 -20 dB gain adjust 0x01 -19.5 dB gain adjust (...) 0x27 -0.5 dB gain adjust 0x28 0 dB gain adjust 0x29 +0.5 dB gain adjust (...) 0x4F +19.5 dB gain adjust 0x50 +20 dB gain adjust
        @ReadWrite(bits: 0..<7, as: GAINL_FIELDValues.self)
        var gainl_field: GAINL_FIELD
    }

    /// Right output gain adjustment
    @Register(bitWidth: 32)
    struct GAINR {
        /// Right output gain adjustment, in 0.5 dB steps, around the default module gain (see electrical parameters)
        @ReadWrite(bits: 0..<7, as: GAINR_FIELDValues.self)
        var gainr_field: GAINR_FIELD
    }

    /// Selects the ratio between PDM_CLK and output sample rate. Change PDMCLKCTRL accordingly.
    @Register(bitWidth: 32)
    struct RATIO {
        /// Selects the ratio between PDM_CLK and output sample rate
        @ReadWrite(bits: 0..<1, as: RATIO_FIELDValues.self)
        var ratio_field: RATIO_FIELD
    }

    /// Unspecified
    @RegisterBlock
    struct PSEL {
        /// Pin number configuration for PDM CLK signal
        @RegisterBlock(offset: 0x0)
        var clk: Register<CLK>

        /// Pin number configuration for PDM DIN signal
        @RegisterBlock(offset: 0x4)
        var din: Register<DIN>
    }

    /// Unspecified
    @RegisterBlock
    struct SAMPLE {
        /// RAM address pointer to write samples to with EasyDMA
        @RegisterBlock(offset: 0x0)
        var ptr: Register<PTR>

        /// Number of samples to allocate memory for in EasyDMA mode
        @RegisterBlock(offset: 0x4)
        var maxcnt: Register<MAXCNT>
    }
}

extension PDM.PSEL {
    /// Pin number configuration for PDM CLK signal
    @Register(bitWidth: 32)
    struct CLK {
        /// Pin number
        @ReadWrite(bits: 0..<5)
        var pin: PIN

        /// Port number
        @ReadWrite(bits: 5..<6)
        var port: PORT

        /// Connection
        @ReadWrite(bits: 31..<32, as: CONNECTValues.self)
        var connect: CONNECT
    }

    /// Pin number configuration for PDM DIN signal
    @Register(bitWidth: 32)
    struct DIN {
        /// Pin number
        @ReadWrite(bits: 0..<5)
        var pin: PIN

        /// Port number
        @ReadWrite(bits: 5..<6)
        var port: PORT

        /// Connection
        @ReadWrite(bits: 31..<32, as: CONNECTValues.self)
        var connect: CONNECT
    }
}

extension PDM.SAMPLE {
    /// RAM address pointer to write samples to with EasyDMA
    @Register(bitWidth: 32)
    struct PTR {
        /// Address to write PDM samples to over DMA
        @ReadWrite(bits: 0..<32)
        var sampleptr: SAMPLEPTR
    }

    /// Number of samples to allocate memory for in EasyDMA mode
    @Register(bitWidth: 32)
    struct MAXCNT {
        /// Length of DMA RAM allocation in number of samples
        @ReadWrite(bits: 0..<15)
        var buffsize: BUFFSIZE
    }
}

extension PDM.TASKS_START {
    struct TASKS_START_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.TASKS_STOP {
    struct TASKS_STOP_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.EVENTS_STARTED {
    struct EVENTS_STARTED_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.EVENTS_STOPPED {
    struct EVENTS_STOPPED_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.EVENTS_END {
    struct EVENTS_END_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.INTEN {
    struct STARTEDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.INTEN {
    struct STOPPEDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.INTEN {
    struct ENDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.ENABLE {
    struct ENABLE_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable
        static let Disabled = Self(rawValue: 0x0)

        /// Enable
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.PDMCLKCTRL {
    struct FREQValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 32

        /// PDM_CLK = 32 MHz / 32 = 1.000 MHz
        static let _1000K = Self(rawValue: 0x8000000)

        /// PDM_CLK = 32 MHz / 31 = 1.032 MHz. Nominal clock for RATIO=Ratio64.
        static let Default = Self(rawValue: 0x8400000)

        /// PDM_CLK = 32 MHz / 30 = 1.067 MHz
        static let _1067K = Self(rawValue: 0x8800000)

        /// PDM_CLK = 32 MHz / 26 = 1.231 MHz
        static let _1231K = Self(rawValue: 0x9800000)

        /// PDM_CLK = 32 MHz / 25 = 1.280 MHz. Nominal clock for RATIO=Ratio80.
        static let _1280K = Self(rawValue: 0xa000000)

        /// PDM_CLK = 32 MHz / 24 = 1.333 MHz
        static let _1333K = Self(rawValue: 0xa800000)

        var rawValue: UInt32

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.MODE {
    struct OPERATIONValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Sample and store one pair (left + right) of 16-bit samples per RAM word R=[31:16]; L=[15:0]
        static let Stereo = Self(rawValue: 0x0)

        /// Sample and store two successive left samples (16 bits each) per RAM word L1=[31:16]; L0=[15:0]
        static let Mono = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.MODE {
    struct EDGEValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Left (or mono) is sampled on falling edge of PDM_CLK
        static let LeftFalling = Self(rawValue: 0x0)

        /// Left (or mono) is sampled on rising edge of PDM_CLK
        static let LeftRising = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.GAINL {
    struct GAINL_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 7

        /// -20 dB gain adjustment (minimum)
        static let MinGain = Self(rawValue: 0x0)

        /// 0 dB gain adjustment
        static let DefaultGain = Self(rawValue: 0x28)

        /// +20 dB gain adjustment (maximum)
        static let MaxGain = Self(rawValue: 0x50)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.GAINR {
    struct GAINR_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 7

        /// -20 dB gain adjustment (minimum)
        static let MinGain = Self(rawValue: 0x0)

        /// 0 dB gain adjustment
        static let DefaultGain = Self(rawValue: 0x28)

        /// +20 dB gain adjustment (maximum)
        static let MaxGain = Self(rawValue: 0x50)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.RATIO {
    struct RATIO_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Ratio of 64
        static let Ratio64 = Self(rawValue: 0x0)

        /// Ratio of 80
        static let Ratio80 = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.PSEL.CLK {
    struct CONNECTValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disconnect
        static let Disconnected = Self(rawValue: 0x1)

        /// Connect
        static let Connected = Self(rawValue: 0x0)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension PDM.PSEL.DIN {
    struct CONNECTValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disconnect
        static let Disconnected = Self(rawValue: 0x1)

        /// Connect
        static let Connected = Self(rawValue: 0x0)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}
