// Generated by svd2swift.

import MMIO

/// Random Number Generator
@RegisterBlock
struct RNG {
    /// Task starting the random number generator
    @RegisterBlock(offset: 0x0)
    var tasks_start: Register<TASKS_START>

    /// Task stopping the random number generator
    @RegisterBlock(offset: 0x4)
    var tasks_stop: Register<TASKS_STOP>

    /// Event being generated for every new random number written to the VALUE register
    @RegisterBlock(offset: 0x100)
    var events_valrdy: Register<EVENTS_VALRDY>

    /// Shortcuts between local events and tasks
    @RegisterBlock(offset: 0x200)
    var shorts: Register<SHORTS>

    /// Enable interrupt
    @RegisterBlock(offset: 0x304)
    var intenset: Register<INTENSET>

    /// Disable interrupt
    @RegisterBlock(offset: 0x308)
    var intenclr: Register<INTENCLR>

    /// Configuration register
    @RegisterBlock(offset: 0x504)
    var config: Register<CONFIG>

    /// Output random number
    @RegisterBlock(offset: 0x508)
    var value: Register<VALUE>
}

extension RNG {
    /// Task starting the random number generator
    @Register(bitWidth: 32)
    struct TASKS_START {
        /// Task starting the random number generator
        @WriteOnly(bits: 0..<1, as: TASKS_START_FIELDValues.self)
        var tasks_start_field: TASKS_START_FIELD
    }

    /// Task stopping the random number generator
    @Register(bitWidth: 32)
    struct TASKS_STOP {
        /// Task stopping the random number generator
        @WriteOnly(bits: 0..<1, as: TASKS_STOP_FIELDValues.self)
        var tasks_stop_field: TASKS_STOP_FIELD
    }

    /// Event being generated for every new random number written to the VALUE register
    @Register(bitWidth: 32)
    struct EVENTS_VALRDY {
        /// Event being generated for every new random number written to the VALUE register
        @ReadWrite(bits: 0..<1, as: EVENTS_VALRDY_FIELDValues.self)
        var events_valrdy_field: EVENTS_VALRDY_FIELD
    }

    /// Shortcuts between local events and tasks
    @Register(bitWidth: 32)
    struct SHORTS {
        /// Shortcut between event VALRDY and task STOP
        @ReadWrite(bits: 0..<1, as: VALRDY_STOPValues.self)
        var valrdy_stop: VALRDY_STOP
    }

    /// Enable interrupt
    @Register(bitWidth: 32)
    struct INTENSET {
        /// Write '1' to enable interrupt for event VALRDY
        @ReadWrite(bits: 0..<1)
        var valrdy: VALRDY
    }

    /// Disable interrupt
    @Register(bitWidth: 32)
    struct INTENCLR {
        /// Write '1' to disable interrupt for event VALRDY
        @ReadWrite(bits: 0..<1)
        var valrdy: VALRDY
    }

    /// Configuration register
    @Register(bitWidth: 32)
    struct CONFIG {
        /// Bias correction
        @ReadWrite(bits: 0..<1, as: DERCENValues.self)
        var dercen: DERCEN
    }

    /// Output random number
    @Register(bitWidth: 32)
    struct VALUE {
        /// Generated random number
        @ReadOnly(bits: 0..<8)
        var value_field: VALUE_FIELD
    }
}

extension RNG.TASKS_START {
    struct TASKS_START_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension RNG.TASKS_STOP {
    struct TASKS_STOP_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Trigger task
        static let Trigger = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension RNG.EVENTS_VALRDY {
    struct EVENTS_VALRDY_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Event not generated
        static let NotGenerated = Self(rawValue: 0x0)

        /// Event generated
        static let Generated = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension RNG.SHORTS {
    struct VALRDY_STOPValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable shortcut
        static let Disabled = Self(rawValue: 0x0)

        /// Enable shortcut
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension RNG.CONFIG {
    struct DERCENValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disabled
        static let Disabled = Self(rawValue: 0x0)

        /// Enabled
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}
