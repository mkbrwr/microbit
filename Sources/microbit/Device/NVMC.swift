// Generated by svd2swift.

import MMIO

/// Non Volatile Memory Controller
@RegisterBlock
struct NVMC {
    /// Ready flag
    @RegisterBlock(offset: 0x400)
    var ready: Register<READY>

    /// Ready flag
    @RegisterBlock(offset: 0x408)
    var readynext: Register<READYNEXT>

    /// Configuration register
    @RegisterBlock(offset: 0x504)
    var config: Register<CONFIG>

    /// Register for erasing a page in code area
    @RegisterBlock(offset: 0x508)
    var erasepage: Register<ERASEPAGE>

    /// Deprecated register - Register for erasing a page in code area, equivalent to ERASEPAGE
    @RegisterBlock(offset: 0x508)
    var erasepcr1: Register<ERASEPCR1>

    /// Register for erasing all non-volatile user memory
    @RegisterBlock(offset: 0x50c)
    var eraseall: Register<ERASEALL>

    /// Deprecated register - Register for erasing a page in code area, equivalent to ERASEPAGE
    @RegisterBlock(offset: 0x510)
    var erasepcr0: Register<ERASEPCR0>

    /// Register for erasing user information configuration registers
    @RegisterBlock(offset: 0x514)
    var eraseuicr: Register<ERASEUICR>

    /// Register for partial erase of a page in code area
    @RegisterBlock(offset: 0x518)
    var erasepagepartial: Register<ERASEPAGEPARTIAL>

    /// Register for partial erase configuration
    @RegisterBlock(offset: 0x51c)
    var erasepagepartialcfg: Register<ERASEPAGEPARTIALCFG>

    /// I-code cache configuration register
    @RegisterBlock(offset: 0x540)
    var icachecnf: Register<ICACHECNF>

    /// I-code cache hit counter
    @RegisterBlock(offset: 0x548)
    var ihit: Register<IHIT>

    /// I-code cache miss counter
    @RegisterBlock(offset: 0x54c)
    var imiss: Register<IMISS>
}

extension NVMC {
    /// Ready flag
    @Register(bitWidth: 32)
    struct READY {
        /// NVMC is ready or busy
        @ReadOnly(bits: 0..<1, as: READY_FIELDValues.self)
        var ready_field: READY_FIELD
    }

    /// Ready flag
    @Register(bitWidth: 32)
    struct READYNEXT {
        /// NVMC can accept a new write operation
        @ReadOnly(bits: 0..<1, as: READYNEXT_FIELDValues.self)
        var readynext_field: READYNEXT_FIELD
    }

    /// Configuration register
    @Register(bitWidth: 32)
    struct CONFIG {
        /// Program memory access mode. It is strongly recommended to only activate erase and write modes when they are actively used. Enabling write or erase will invalidate the cache and keep it invalidated.
        @ReadWrite(bits: 0..<2, as: WENValues.self)
        var wen: WEN
    }

    /// Register for erasing a page in code area
    @Register(bitWidth: 32)
    struct ERASEPAGE {
        /// Register for starting erase of a page in code area
        @WriteOnly(bits: 0..<32)
        var erasepage_field: ERASEPAGE_FIELD
    }

    /// Deprecated register - Register for erasing a page in code area, equivalent to ERASEPAGE
    @Register(bitWidth: 32)
    struct ERASEPCR1 {
        /// Register for erasing a page in code area, equivalent to ERASEPAGE
        @WriteOnly(bits: 0..<32)
        var erasepcr1_field: ERASEPCR1_FIELD
    }

    /// Register for erasing all non-volatile user memory
    @Register(bitWidth: 32)
    struct ERASEALL {
        /// Erase all non-volatile memory including UICR registers. The erase must be enabled using CONFIG.WEN before the non-volatile memory can be erased.
        @WriteOnly(bits: 0..<1, as: ERASEALL_FIELDValues.self)
        var eraseall_field: ERASEALL_FIELD
    }

    /// Deprecated register - Register for erasing a page in code area, equivalent to ERASEPAGE
    @Register(bitWidth: 32)
    struct ERASEPCR0 {
        /// Register for starting erase of a page in code area, equivalent to ERASEPAGE
        @WriteOnly(bits: 0..<32)
        var erasepcr0_field: ERASEPCR0_FIELD
    }

    /// Register for erasing user information configuration registers
    @Register(bitWidth: 32)
    struct ERASEUICR {
        /// Register starting erase of all user information configuration registers. The erase must be enabled using CONFIG.WEN before the UICR can be erased.
        @WriteOnly(bits: 0..<1, as: ERASEUICR_FIELDValues.self)
        var eraseuicr_field: ERASEUICR_FIELD
    }

    /// Register for partial erase of a page in code area
    @Register(bitWidth: 32)
    struct ERASEPAGEPARTIAL {
        /// Register for starting partial erase of a page in code area
        @WriteOnly(bits: 0..<32)
        var erasepagepartial_field: ERASEPAGEPARTIAL_FIELD
    }

    /// Register for partial erase configuration
    @Register(bitWidth: 32)
    struct ERASEPAGEPARTIALCFG {
        /// Duration of the partial erase in milliseconds
        @ReadWrite(bits: 0..<7)
        var duration: DURATION
    }

    /// I-code cache configuration register
    @Register(bitWidth: 32)
    struct ICACHECNF {
        /// Cache enable
        @ReadWrite(bits: 0..<1, as: CACHEENValues.self)
        var cacheen: CACHEEN

        /// Cache profiling enable
        @ReadWrite(bits: 8..<9, as: CACHEPROFENValues.self)
        var cacheprofen: CACHEPROFEN
    }

    /// I-code cache hit counter
    @Register(bitWidth: 32)
    struct IHIT {
        /// Number of cache hits. Register is writable, but only to '0'.
        @ReadWrite(bits: 0..<32)
        var hits: HITS
    }

    /// I-code cache miss counter
    @Register(bitWidth: 32)
    struct IMISS {
        /// Number of cache misses. Register is writable, but only to '0'.
        @ReadWrite(bits: 0..<32)
        var misses: MISSES
    }
}

extension NVMC.READY {
    struct READY_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// NVMC is busy (on-going write or erase operation)
        static let Busy = Self(rawValue: 0x0)

        /// NVMC is ready
        static let Ready = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension NVMC.READYNEXT {
    struct READYNEXT_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// NVMC cannot accept any write operation
        static let Busy = Self(rawValue: 0x0)

        /// NVMC is ready
        static let Ready = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension NVMC.CONFIG {
    struct WENValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 2

        /// Read only access
        static let Ren = Self(rawValue: 0x0)

        /// Write enabled
        static let Wen = Self(rawValue: 0x1)

        /// Erase enabled
        static let Een = Self(rawValue: 0x2)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension NVMC.ERASEALL {
    struct ERASEALL_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// No operation
        static let NoOperation = Self(rawValue: 0x0)

        /// Start chip erase
        static let Erase = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension NVMC.ERASEUICR {
    struct ERASEUICR_FIELDValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// No operation
        static let NoOperation = Self(rawValue: 0x0)

        /// Start erase of UICR
        static let Erase = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension NVMC.ICACHECNF {
    struct CACHEENValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable cache. Invalidates all cache entries.
        static let Disabled = Self(rawValue: 0x0)

        /// Enable cache
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}

extension NVMC.ICACHECNF {
    struct CACHEPROFENValues: BitFieldProjectable, RawRepresentable {
        static let bitWidth = 1

        /// Disable cache profiling
        static let Disabled = Self(rawValue: 0x0)

        /// Enable cache profiling
        static let Enabled = Self(rawValue: 0x1)

        var rawValue: UInt8

        @inlinable @inline(__always)
        init(rawValue: Self.RawValue) {
            self.rawValue = rawValue
        }
    }
}
